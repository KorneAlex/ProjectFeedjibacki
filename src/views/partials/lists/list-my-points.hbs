{{!-- Maps disabled in frontend templates
<div style="max-height: 40rem; overflow-y: auto;">
    <table class="table is-fullwidth centered__table is-striped is-narrow">
        <tr>
            <th>Product</th>
            <th>Feedback</th>
            <th>Latitude</th>
            <th>Longitude</th>
            <th>Created</th>
            <th>Category</th>
            <th>Actions</th>
        </tr>
    {{#each viewData.points}}
    <tr>
    <td>{{this.data.name}}</td>
    <td>{{this.data.description}}</td>
    <td>{{this.pos.lat}}</td>
    <td>{{this.pos.lon}}</td>
    <td>{{this.time.created}}</td>
    <td>{{#if this.data.categories}}{{this.data.categories}}{{else}}No categories{{/if}}</td>
            <td class="is-flex is-justify-content-center">
            <a href="/point?id={{this._id}}" title="View feedback" class="button list-button">{{>svg/point }}</a>
            <a href="/point?id={{this._id}}" title="Delete feedback" class="button list-button">{{>svg/delete }}</a>
            </td>
        </tr>
    {{/each}}
    </table>
</div>
--}}
